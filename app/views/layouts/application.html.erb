<!DOCTYPE html>
<html>
<head>
  <title>BanjoChallenge</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body style="background: #3D3D5C">

<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" style="color: purple" href="#">Banjo Challenge</a>
      <form action="/photo_sets" method = "post" class="navbar-search pull-right">
	      	<input 
	      	type="hidden"
	      	name="authenticity_token"
	      	value="<%=form_authenticity_token%>">
		      <div style="display: inline-block">
						<label for="set_location">Latitude, logitude:</label>
						<input class="search-query" placeholder='e.g. "37.4828, 122.2361"' type="text" id="set_location" name="set[location]" value="<%=(@set) ? "#{@set.lat}, #{@set.lng}" : ''%>">
					</div>
				 	<div style="display: inline-block">
						<label for="set_radius">Search Radius (in km):</label>
						<input class="search-query" type="text" placeholder='e.g. "5"' id="set_radius" name="set[radius]" value="<%=(@set) ? @set.radius : ''%>" >
						<input class="btn btn-primary" type="submit" value="Search">
	    		</div>
	    		<%unless flash[:errors].nil?%>
		    		<%[*flash[:errors]].each do |error|%>
		    			<p style="color: red;"><%=error%></p>
		    		<%end%>
		    	<%end%>
			</form>
  </div>
</div>



<%= yield %>

</body>
</html>
